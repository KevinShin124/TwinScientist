# TwinScientist 环境变量配置
# 复制为 .env 后填入真实值

# 阿里云百炼平台 API Key (必填)
BAILIAN_API_KEY=sk-your-key-here

# 可选：自定义模型名称 (默认 qwen-max)
MODEL_NAME=qwen-max

# 可选：最大迭代次数 (默认 200，此处可改为其他值覆盖代码默认)
MAX_ITERATIONS=200

# 可选：是否启用人工审批断点 (默认 true)
HUMAN_APPROVAL_ENABLED=false
